@model Ach.Fulfillment.Web.Areas.Manage.Models.PartnerModel

@{
    ViewBag.Title = "Create Partner";
}

@using (Html.BeginForm("Create", "Partner", new { area = "Manage" }))
{
    <fieldset>
        <legend>Create Partner</legend>

        @Html.Partial("_partnerDetails", this.Model)
        
         <div class="btn-toolbar">
            <div class="btn-group">
                <input type="submit" value="Create" class="btn btn-large" />
                @Html.ActionLink("Back to List", "List", "Partner", new { @class = "btn btn-large" })
             </div>
        </div>
    </fieldset>
}

@section scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
